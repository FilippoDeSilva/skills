{
  "name": "owasp-security-scripts",
  "version": "1.0.0",
  "type": "module",
  "description": "Build scripts for OWASP Security Best Practices skillset",
  "scripts": {
    "build": "npx ts-node build-agents.ts",
    "build:watch": "npx nodemon --watch ../rules --ext md --exec 'npx ts-node build-agents.ts'"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.0.0"
  }
}
